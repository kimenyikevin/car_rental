version: '3.8'
services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: kimenyi
      POSTGRES_PASSWORD: password
      POSTGRES_DB: clickbar
    volumes:
      - ./data:/var/lib/postgresql/data
